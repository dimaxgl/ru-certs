version: "2"

run:
  timeout: 5m
  tests: true

linters:
  enable:
    - gofmt
    - govet
    - ineffassign
    - staticcheck
    - errcheck
    - gosimple
    - unused

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0
